use sum::sum;
use util::sum::sum;

fn main() {
    // marco
    println!("Hello, world!");

    let mut n = 5;
    - ownership
    - shadow: chi xem
    - mutual: cho phep chinh sua

    let m = n;
    m = m + 2;

    println!("{}", sum(n, m));

    // Garbage Collector:

    // Stack vs Heap
}


